# Funciones de Transferencia

En esta clase se abordará el tema de las funciones de transferencia, un concepto fundamental en el campo de los sistemas de control. Las funciones de transferencia permiten analizar y diseñar sistemas dinámicos, facilitando la comprensión de la relación entre las entradas y salidas de un sistema. A través de la transformada de Laplace, se pueden representar matemáticamente las ecuaciones diferenciales que describen el comportamiento de estos sistemas, lo que resulta esencial para el control y la estabilidad de procesos industriales y tecnológicos.

## 1. Introducción a las Funciones de Transferencia

Las funciones de transferencia son representaciones matemáticas que relacionan la salida de un sistema con su entrada en el dominio de la frecuencia. Este concepto es crucial para el análisis de sistemas de control, ya que permite simplificar el estudio de sistemas complejos mediante el uso de herramientas matemáticas. En esencia, una función de transferencia proporciona una forma de entender cómo un sistema responde a diferentes entradas, lo que es fundamental para el diseño de controladores que mantengan el sistema en un estado deseado.

## 2. Definiciones

🔑 _Función de Transferencia_: Es la transformada de Laplace de la ecuación diferencial que describe un sistema, representando la relación entre la salida y la entrada del mismo. Se expresa generalmente como $$\( G(s) = \frac{Y(s)}{U(s)} \)$$, donde $$\( Y(s) \)$$ es la salida y $$\( U(s) \)$$ es la entrada en el dominio de Laplace.

## 3. Componentes de un Sistema de Control

### 3.1. Planta
La planta es el componente físico que realiza el proceso de control. Puede ser un tanque, un motor, o cualquier sistema que se desee controlar. La planta es donde ocurre la transformación de la entrada en salida. Por ejemplo, en un sistema de control de temperatura, la planta sería el horno que calienta el aire.

### 3.2. Actuador
El actuador es el dispositivo que recibe la señal de control y actúa sobre la planta para modificar su comportamiento. Por ejemplo, en un sistema de control de temperatura, el actuador podría ser una válvula que regula el flujo de aire caliente. La efectividad del actuador es crucial para la respuesta del sistema, ya que debe ser capaz de realizar ajustes rápidos y precisos en respuesta a las señales del controlador.

### 3.3. Sensor
El sensor mide la salida del sistema y proporciona información sobre el estado actual de la planta. En el caso del control de temperatura, un termopar podría ser utilizado para medir la temperatura del aire en el horno. La precisión y la rapidez del sensor son vitales, ya que cualquier retraso o error en la medición puede afectar la estabilidad y el rendimiento del sistema.

### 3.4. Controlador
El controlador toma decisiones basadas en la comparación entre la salida medida y el valor deseado, generando la señal de control adecuada. Existen diferentes tipos de controladores, como el controlador proporcional (P), integral (I) y derivativo (D), que pueden ser combinados en un controlador PID. La elección del controlador adecuado es fundamental para garantizar que el sistema responda de manera efectiva a las perturbaciones y mantenga la salida en el valor deseado.

## 4. Ejemplos

💡 **Ejemplo 1**: Consideremos un sistema de control de temperatura en un horno. 

### 4.1. Descripción del Sistema
- **Planta**: Horno
- **Entrada**: Caudal de aire
- **Sensor**: Termopar
- **Salida**: Temperatura
- **Actuador**: Válvula

### 4.2. Funcionamiento del Sistema
1. **Entrada**: Se introduce un caudal de aire al horno, que se considera la entrada del sistema. Esta entrada puede ser controlada manualmente o automáticamente. El caudal de aire afecta directamente la temperatura del horno, por lo que es esencial regularlo adecuadamente.

2. **Sensor**: Un termopar mide la temperatura actual dentro del horno. Este sensor convierte la temperatura en una señal eléctrica que se envía al controlador. La señal del sensor debe ser precisa y rápida para que el controlador pueda reaccionar a tiempo ante cualquier cambio en la temperatura.

3. **Controlador**: El controlador compara la temperatura medida (salida) con la temperatura deseada (setpoint). Si hay una diferencia, el controlador calcula el error y decide cómo ajustar la entrada para minimizar este error. Por ejemplo, si la temperatura medida es inferior a la deseada, el controlador puede aumentar el caudal de aire.

4. **Actuador**: Basado en la decisión del controlador, el actuador (válvula) ajusta el caudal de aire que entra al horno. Si la temperatura es demasiado baja, la válvula se abre más para permitir un mayor flujo de aire caliente. Si la temperatura es demasiado alta, la válvula se cierra. La rapidez y precisión del actuador son cruciales para mantener la temperatura en el nivel deseado.

### 4.3. Función de Transferencia del Sistema
Para analizar este sistema, se puede establecer una ecuación diferencial que relacione la entrada y la salida. Supongamos que la dinámica del horno se puede modelar con la siguiente ecuación diferencial:

$$
\tau \frac{dT(t)}{dt} + T(t) = K \cdot U(t)
$$

donde:
- \(T(t)\) es la temperatura en el tiempo \(t\),
- \(U(t)\) es el caudal de aire,
- \(K\) es la ganancia del sistema,
- $$\(\tau\)$$ es la constante de tiempo del sistema.

### 4.4. Transformada de Laplace
Aplicando la transformada de Laplace a la ecuación diferencial, se obtiene:

$$
\tau s T(s) + T(s) = K \cdot U(s)
$$

Despejando \(T(s)\):

$$
T(s) = \frac{K}{\tau s + 1} U(s)
$$

### 4.5. Interpretación de la Función de Transferencia
La función de transferencia $$\(G(s) = \frac{T(s)}{U(s)} = \frac{K}{\tau s + 1}\)$$ describe cómo la salida (temperatura) responde a la entrada (caudal de aire). 

- **Polos**: El término $$\(\tau s + 1 = 0\)$$ tiene un polo en $$\(s = -\frac{1}{\tau}\)$$, lo que indica la estabilidad del sistema. Un polo en el semiplano izquierdo sugiere que el sistema es estable, mientras que un polo en el semiplano derecho indicaría inestabilidad.

- **Ceros**: En este caso, no hay ceros en la función de transferencia, lo que significa que no hay valores de $$\(s\)$$ que hagan que el numerador sea cero. Esto implica que el sistema no tiene un comportamiento de respuesta instantánea a la entrada.

### 4.6. Respuesta del Sistema
La respuesta del sistema a una entrada escalón (un cambio repentino en el caudal de aire) se puede analizar utilizando la función de transferencia. La respuesta temporal se puede obtener aplicando la transformada inversa de Laplace a $$\(T(s)\)$$, lo que permite visualizar cómo la temperatura del horno se ajusta a lo largo del tiempo después de un cambio en la entrada.

## 5. Ecuaciones
Las funciones de transferencia se pueden expresar como:

$$ G(s) = \frac{Y(s)}{U(s)} $$

donde (Y(s)) es la transformada de Laplace de la salida y (U(s)) es la transformada de Laplace de la entrada.

### 5.1. Derivación de la Función de Transferencia
Para entender mejor cómo funcionan las ecuaciones de las funciones de transferencia, consideremos el proceso de derivación de la función de transferencia a partir de una ecuación diferencial.

- **Ecuación Diferencial:** Comenzamos con una ecuación diferencial que describe el sistema. Por ejemplo, la ecuación del horno:

$$\tau \frac{dT(t)}{dt} + T(t) = K \cdot U(t)$$

- **Aplicación de la Transformada de Laplace:** Aplicamos la transformada de Laplace a cada término de la ecuación. Recordemos que la transformada de Laplace de una derivada es:

$$
\mathcal{L}\left(\frac{dy(t)}{dt}\right) = sY(s) - y(0)
$$

Aplicando esto a nuestra ecuación, obtenemos:

$$ \tau (sT(s) - T(0)) + T(s) = K \cdot U(s) $$

Despeje de T(s): Reorganizamos la ecuación para despejar (T(s)):

$$ \tau s T(s) + T(s) = K \cdot U(s) + \tau T(0) $$

Factorizando (T(s)):

$$ T(s)(\tau s + 1) = K \cdot U(s) + \tau T(0) $$

Finalmente, despejamos (T(s)):

$$ T(s) = \frac{K \cdot U(s) + \tau T(0)}{\tau s + 1} $$

- **Función de Transferencia:** La función de transferencia se define como la relación entre la salida y la entrada en el dominio de Laplace, ignorando las condiciones iniciales (asumiendo que son cero):

$$ G(s) = \frac{T(s)}{U(s)} = \frac{K}{\tau s + 1} $$

### 5.2. Utilidad de la Función de Transferencia
La función de transferencia es útil por varias razones:

- **Análisis de Estabilidad:** Permite determinar la estabilidad del sistema al analizar la ubicación de los polos en el plano complejo. Un sistema es estable si todos los polos están en el semiplano izquierdo.

- **Respuesta en Frecuencia:** Facilita el análisis de la respuesta en frecuencia del sistema, lo que es esencial para el diseño de controladores. Se pueden utilizar técnicas como el diagrama de Bode para evaluar cómo el sistema responde a diferentes frecuencias de entrada.

- **Diseño de Controladores:** Proporciona una base para el diseño de controladores, como PID, que pueden ajustarse para lograr un rendimiento deseado en términos de tiempo de respuesta, sobreimpulso y estabilidad.

- **Simulación:** Permite simular el comportamiento del sistema en diferentes condiciones, lo que es útil para la validación del diseño antes de implementar el sistema en la práctica.

## 6. Figuras

Para ilustrar el concepto de funciones de transferencia, se puede incluir un diagrama de bloques que represente un sistema de control típico.

```
![Diagrama de Bloques](imagenes/diagrama_bloques.png)
```
Figura 1. Diagrama de bloques de un sistema de control.

## 7. Tablas

A continuación, se presenta una tabla que clasifica las funciones de transferencia según su grado:

| Tipo de Función de Transferencia | Grado del Numerador (n) | Grado del Denominador (m) |
|----------------------------------|--------------------------|----------------------------|
| Impropria                        | n > m                    |                            |
| Estrictamente Propia             | m > n                    |                            |
| Bipropia                         | n = m                    |                            |

Tabla 1. Clasificación de funciones de transferencia.

## 8. Código

A continuación, se presenta un segmento de código en MATLAB que puede ser utilizado para calcular la función de transferencia de un sistema:

```matlab
num = [K]; % Numerador
den = [tau 1]; % Denominador
sys = tf(num, den); % Función de transferencia
```

## 9. Ejercicios

📚 **Ejercicio 1**: Hallar la función de transferencia para el siguiente sistema descrito por la ecuación diferencial:

$$
\frac{d^2y(t)}{dt^2} + 3\frac{dy(t)}{dt} + 2y(t) = 3\frac{du(t)}{dt} + 3u(t)
$$

**Solución**: Aplicando la transformada de Laplace y despejando, se obtiene:

$$
Y(s) = \frac{3s - 1}{s^2 + 3s + 2}
$$

📚 **Ejercicio 2**: Determinar los polos y ceros de la función de transferencia obtenida en el ejercicio anterior.

**Solución**: Los ceros se obtienen al igualar el numerador a cero y los polos al igualar el denominador a cero.

## 10. Conclusiones

En esta clase se ha explorado el concepto de funciones de transferencia, su importancia en el análisis de sistemas de control y cómo se relacionan las entradas y salidas de un sistema. Se ha aprendido a representar matemáticamente estos sistemas y a aplicar la transformada de Laplace para simplificar el análisis. Además, se ha discutido el funcionamiento de un sistema de control de temperatura, proporcionando un ejemplo práctico que ilustra la aplicación de las funciones de transferencia en un contexto real.

## 11. Referencias

- Cote Ballesteros, J. E. (2023). Funciones de Transferencia. M7A Sistemas de Control I.
- Material adicional sobre transformadas de Laplace y funciones de transferencia.
